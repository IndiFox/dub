{
  "installCommand": "cd ../.. && pnpm install --no-frozen-lockfile",
  "buildCommand": "cd ../.. && NODE_OPTIONS=--max-old-space-size=6144 pnpm build --filter=web",
  "env": {
    "ENABLE_ROOT_PATH_BUILD_CACHE": "1"
  }
}
